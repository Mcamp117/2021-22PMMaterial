public class Bool {
    public static void main(String[] args) {
        // BOOLEAN EXPRESSION = Any expression that returns a BOOLEAN;

        //String name;
        //ArrayList al;
        //List l;
        
        int x;
        double y;
        // boolean z;  //booleans are primitive
        // char c;
        // byte b;

        boolean z;

        a=true;
        

        /* relational operator 
            = > < <= >= && || != ==
            && is and
            || is or
            != not equal to
            == is it equal to 
        */

        boolean a;
        boolean b = true;   //python = True
        boolean c = false;  //python = False

        a= a!=b;
        b= a==b;
        c= !(a==b);

        System.out.println(a);
        System.out.println(b);
        System.out.println(c);

        int d = 4;
        int e = 5;
        System.out.println(d<e);
    }
}
